[project]
name = "f1-bayesian"
version = "0.1.0"
description = ""
authors = [
    {name = "Klaudiusz Grobelski, Łukasz Filo"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastf1 (>=3.5.3,<4.0.0)"
]

[tool.poetry]
packages = [{include = "f1_bayesian", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
